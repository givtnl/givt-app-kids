import 'package:flutter/material.dart';

class TestLinkSCreen extends StatelessWidget {
  const TestLinkSCreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Test HERE'),
        ),
        body: Center(
          child: Text('Test Link Screen'),
        ));
  }
}
